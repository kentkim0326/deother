# Deother 사이트 — 세션 인수인계 (2026-08-10, 노트북 반납 전)

> 이 노트북을 반납하기 전 마지막 정리다. 사무실 새 컴퓨터에서 **바로 이어서** 하기 위한 문서.
> 전체 맥락은 `CLAUDE.md`(항상 최신). 이 파일은 **① 반납 전 필수 체크 ② 최근 한 것 ③ 바로 다음**.

---

## 0. ★ 반납 전 필수 — 안 하면 유실된다

노트북(F 드라이브 포함)을 반납하면 **원격(GitHub)에 없는 것은 전부 사라진다.**
아래 두 곳은 로컬에만 있는 변경이 있다. **반납 전 대표님이 직접 처리할 것:**

| 프로젝트 | 문제 | 조치 |
|---|---|---|
| **F:\Reaper** (Death Guide 게임 소스) | **미커밋 19개** | `cd F:\Reaper` → `git add -A` → `git commit -m "..."` → `git push` |
| **F:\Projects\FightingNations\FightingNations** | **로컬 커밋 2개 미푸시** | `git push` (커밋은 돼 있고 push 만 안 됨) |

그리고 **게임 Content 폴더는 git 에 없는 단일 사본**인 프로젝트가 있다(특히 Soomba 11GB).
소스 코드는 git 에 있어도 **Content(에셋)는 별도 백업**해야 반납 후 복구된다 — 각 게임의
`.gitignore` 를 확인하고, 필요한 에셋은 외장/클라우드로 옮길 것.

### 반납 전 확인 완료된 것 (안전 — 원격에 다 있음)
| 프로젝트 | 위치 | 원격 | 상태 |
|---|---|---|---|
| **deother (이 사이트)** | `F:\deother` | `kentkim0326/deother` | ✅ 최신·푸시됨 |
| Fighting Nations | `F:\Projects\FightingNations\FightingNations` | `kentkim0326/FightingNations` | ⚠️ 위 참고(미푸시 2) |
| KW_MMO (KWO) | `F:\KW_MMO` | `kentkim0326/KW-MMO` | ✅ |
| Death Guide | `F:\Reaper` | `kentkim0326/DeathGuide` | ⚠️ 위 참고(미커밋 19) |
| Soomba | `F:\Soomba\Zombie` | `kentkim0326/soomba` | ✅ (Content 11GB 는 git 밖) |
| Time Wars | `F:\TimeWars\TimeWars` | `kentkim0326/TimeWars` | ✅ |
| Turf King | `F:\TurfKing` | `kentkim0326/Turf-King` | ✅ (기획만, 코드 0줄) |

---

## 1. 사무실 복귀 후 — 30초 안에 이어받기

```bash
git clone https://github.com/kentkim0326/deother.git
cd deother
```

빌드도 설치도 없다. `git push` 하면 Vercel 이 1~2분 안에 자동 배포한다.
확인만 하려면 서버로 띄운다(`file://` 로 직접 열면 빈 화면):

```powershell
# python 은 이 PC 에서 Windows Store 스텁이라 안 된다. Node 가 있으면:
npx serve . -l 5173
```

그 다음 **`CLAUDE.md` 를 먼저 읽는다** — 파일 구조, 렌더 방식, 함정, 게임별 ⚠️,
진행 상황, 다음 세션 시작점이 전부 거기 있다. 이 파일은 그 위에 "최근 + 바로 다음"만 얹는다.

---

## 2. 이 노트북에서 한 것 (최근 세션)

**게임 5종 → 9종.** 새로 4종을 사이트에 올렸다:

| 게임 | 카드 | 상세 | 특이 |
|---|---|---|---|
| 숨바 (Soomba) | 10개 언어 | ko/en | 캐릭터 일러스트, itch Public |
| 저승사자 (Death Guide) | 10개 언어 | ko/en | 슬라이드쇼 9장, **MATURE** |
| 타임 워즈 (Time Wars) | 10개 언어 | ko/en | 게임플레이 영상 + Coldplay 배경음 |
| 터프 킹 (Turf King) | 10개 언어 | ko/en | **개발 중(기획만)**, 로스터 20명, English 배경음, **MATURE** |

그 밖에:
- **KWO 서버 Fly.io 상시 호스팅 완료** — 터널 졸업, 고정 주소 `wss://kw-mmo-server.fly.dev`.
  대표님 PC 를 꺼도 서버가 산다. 관리는 `F:\KW_MMO\server\DEPLOY.md`.
- **KWO 상세**: 영어 주제가 영상 3편 + 진입 EDM 배경음 + 실제 모바일 플레이 화면 2장.
- **Turf King 로스터 20명** — 도시별 캐릭터를 본문 섹션 사이에 인터리브로 분산(`{roster}`).
- **FN origin 섹션에 남자 캐릭터 영상** (텍스트 긴 곳 글벽 끊기).
- **게임 소개서 PDF** — `F:\Deother_게임소개서.pdf` (Chrome headless 로 생성. 반납 전 백업 권장).
- Soomba/Death Guide/Turf King **성인용(MATURE) 표기**, Death Guide 웹소설 스포일러선.

**현재 사이트**: `deother.vercel.app` · 게임 **9종** · 상세페이지 **8종** · 10개 언어.

---

## 3. 바로 다음에 할 것 (우선순위)

`CLAUDE.md` 의 "다음 세션 시작점" 이 기준. 요약하면:

1. **숨바·저승사자·타임워즈·터프킹 상세 10개 언어 확장** — 지금 ko/en 뿐이다(카드는 10개 언어).
   다른 상세(밀리버스/킹덤워즈/장보고/KWO)처럼 나머지 8개 언어로. `pages-i18n.js`.
2. **itch 콘텐츠 등급** — Death Guide·Turf King 은 성인용인데 itch 페이지 설정에서
   adult content/Gore·Violent 태그를 켜야 사이트와 일관된다. **대표님이 itch 에서 직접.**
3. **Turf King 마감** — 노래 Metallica 버전으로 바꿀지, 언어별 영상(로컬 mp4)을 유튜브에
   올려 상세에 연결할지. 플레이 빌드가 나오면 카드에 다운로드/sns 추가, CTA 를 '팔로우→다운로드'.
4. **KWO 전용 로고** (지금 기존 Kingdom Wars 로고 임시), **밀리버스 실게임 캡처**(대표 로그인 필요).
5. **GWB Game Awards 마감 2026-08-15** — FN 스크린샷 3~5장 + 7번 캐릭터 복장.

---

## 4. deother 작업 시 꼭 기억할 것 (함정 요약 — 상세는 CLAUDE.md)

- 새 게임 = **i18n.js(WORKS+카드 10개 언어) + pages-i18n.js(PAGES) + page.js(P_LAYOUT/pSlideSet)
  + xxx.html + 이미지**. 검증은 `node`(vm 으로 로드) + 브라우저 DOM 조회.
- **없는 요소를 만지면 렌더 전체가 죽는다** → `set()` 헬퍼. 미구현을 구현인 척 쓰지 않는다.
- 커밋 메시지는 `git commit -F <파일>`, 파일은 `[System.IO.File]::WriteAllText(...UTF8 no-BOM)`.
- 게임 본체 레포는 **대표님이 각자 커밋**한다. 나는 deother 만 커밋한다.
